class Solution:
    def isPerfectSquare(self, num: int) -> bool:
        if(num==1):
            return True
        m=num//2
        i=1
        while(i<=m):
            if(i*i==num):
                return True
            elif(i*i>num):
                return False
            else:
                i+=1
                
        
